# Programmering eksamen - overblik

De her noter er tænkt som hurtige huskenoter. Ikke fuld teori, bare de ting der er gode at kunne finde hurtigt under forberedelse eller eksamen.

Typisk workflow i vores projekter:

```sh
cmake -S . -B build
cmake --build build
./build/program
```

Hvis der er tests:

```sh
cmake -S . -B build
cmake --build build
./build/tests
```

Hvis projektet har scripts:

```sh
./run_pub.sh
./run_sub.sh
./run_catch.sh
```

Ekstra repo-eksempler:

- [libsodium-cryptography-demo](https://github.com/mwsk75996/libsodium-cryptography-demo) - password hashing, CHAP og encryption demos
- [q_api_case](https://github.com/mwsk75996/q_api_case) - GoogleTest, black box-test og GitHub Actions CI/CD
- [challenges_helloworld](https://github.com/mwsk75996/challenges_helloworld) - små C++ begynderøvelser
- `mariadb_example/` - MariaDB C++ client eksempel med CMake

Gode ting at tjekke når noget fejler:

- Er du i den rigtige mappe?
- Findes `CMakeLists.txt` i projektroden?
- Er `build/` blevet lavet?
- Hedder executable det samme som target i `CMakeLists.txt`?
- Mangler du et library i `target_link_libraries(...)`?
- Kører MQTT broker på `localhost:1883`?
- Har shell scriptet execute permission? `chmod +x script.sh`

Flere noter:

- `11-googletest-ci.md`
- `12-databaser-mariadb.md`
